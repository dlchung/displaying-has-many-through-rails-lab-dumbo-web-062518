<h1><%= @doctor.name %></h1>
<p>Department: <%= @doctor.department %></p>

<h2>Appointments</h2>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= link_to appointment.nice_date, appointment %> - <%= link_to appointment.patient.name, appointment.patient %></li>
  <% end %>
</ul>
