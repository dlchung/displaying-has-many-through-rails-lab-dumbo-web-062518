<h1><%= @patient.name %></h1>
<h2>Appointments</h2>

<ul>
  <% @patient.appointments.each do |appointment| %>
    <li><%= link_to appointment.nice_date, appointment %> with <%= link_to appointment.doctor.name, appointment.doctor %></li>
  <% end %>
</ul>
